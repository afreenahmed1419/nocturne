// Switch to HeroLayered once you've added all 7 PNGs to /public/hero/
import HeroFallback from "@/components/hero/HeroFallback";

export default function HeroSection() {
  return <HeroFallback />;
}
